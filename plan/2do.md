# 2 do

## Instruction
Treat each item below as a new feature.

## 2 do list
- (DONE) Countries should show a dropdown with all possible countries and show a table of castles of the Netherlands as defauls. The current tables should move to menu items on the top orange bar, called "Top countries" and "Top Regions"
- (DONE) tables should only fill the whole width when required by their content, otherwise just center them
- (DONE) tables that have additional width space available could have additional columns. Propose a few ideas for additional columns and implement one of them.
- (DONE) fix color schema for drop down menu
- (DONE) make the cards view default for all pages
- (DONE) Castle Types page should show City Castle as default
- (DONE) Make smarter use of the available space for the search boxes, now in big screens there is a lot of space to the right of the search boxes and the search results is only visible after scrolling.
- (DONE) make sure the app is responsive for mobile devices. align tables and other elements to the left. put banner image on top of the orange bar in the center if screen gets too small.
- (DONE) add the missing no castles page (refer to old_app)
- (DONE) add an image and description of the selected location type/building concept or condition. use input from the Background page
- (DONE) prepare deployment to the local synology server with a deployment script.
- (DONE) the hamburger button is hidden on ≥768px via styles.scss, which removes sidenav access on desktop. Worth reviewing since the sidenav still holds All Castles, Background, and Visitors links
- (DONE) change page url for Top 1000 to top1000 (now still top100)
- (DONE) Add filters on the top of the tables to filter on the columns of the table, so also for the cards view. The filter should be a drop down menu with all possible values for the column. The filter should be applied to the table when the user selects a value from the drop down menu. The space for the filter should be reserved in the table header or as a separate row above the cards.
- (DONE) remove the visitors page, it is not needed anymore
- (DONE) add 110x110px images for all castles in No Castles page and use the same table setup as the other pages, also remove the whole column Description as it does not add any value. Also remove the description in the details page of the castle (only for no castle)
- (DONE) move the external links part on the castle above the images, otherwise it is not clear that there is additional info below the images
- (DONE) move no castles menu item to dropdown of Castle Types
- (DONE) Refer to the archived version of the old app for the layout of the castle types page ([archived](https://web.archive.org/web/20191210234407/http://topkastelen.nl/soorten.php?Language=en)) The location types have no drawings (this applies also to Castle Type page). A 110x110px image of the castle type should be added to the tables (this does not apply to Castle Type page).
