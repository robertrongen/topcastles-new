# 2 do

## Instruction
Treat each item below as a new feature.

## 2 do list
- (DONE) Countries should show a dropdown with all possible countries and show a table of castles of the Netherlands as defauls. The current tables should move to menu items on the top orange bar, called "Top countries" and "Top Regions"
- (DONE) tables should only fill the whole width when required by their content, otherwise just center them
- (DONE) tables that have additional width space available could have additional columns. Propose a few ideas for additional columns and implement one of them.
- change page url for Top 1000 to top1000 (now still top100)
- fix color schema for drop down menu
- make the cards view default for all pages
- Castle Types page should show City Castle as default
- Make smarter use of the available space for the search boxes, now in big screens there is a lot of space to the right of the search boxes and the search results is only visible after scrolling.
- make sure the app is responsive for mobile devices. align tables and other elements to the left. put banner image on top of the orange bar in the center if screen gets too small.
- add the missing no castles page (refer to old_app)
- add an image and description of the selected location type/building concept or condition. use input from the Background page
- prepare deployment to the local synology server with a deployment script.
- Add filters on the top of the tables to filter on the columns of the table, so also for the cards view. The filter should be a drop down menu with all possible values for the column. The filter should be applied to the table when the user selects a value from the drop down menu. The space for the filter should be reserved in the table header or as a separate row above the cards.