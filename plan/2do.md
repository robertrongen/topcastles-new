# 2 do

## Instruction
Treat each item below as a new feature.

## 2 do list
- Countries should show a dropdown with all possible countries and show a table of castles of the Netherlands as defauls. The current tables should move to menu items on the top orange bar, called "Top countries" and "Top Regions"
- tables should only fill the whole width when required by their content, otherwise just center them
- tables that have additional width space available could have additional columns. Propose a few ideas for additional columns and implement one of them.
- add the missing no castles page (refer to old_app)
- add an image and description of the selected location type/building concept or condition. use input from the Background page
- make sure the app is responsive for mobile devices.
- prepare deployment to the local synology server with a deployment script.